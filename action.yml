name: "GitHub Actions for Dropbox"
description: "📦 Collection of GitHub Actions for Dropbox API. (Upload, Download file, Download .zip)"
branding:
  icon: upload-cloud
  color: blue

runs:
  using: "composite"
  steps:
    - name: Log
      uses: actions/github-script@v7
      with:
        script: |
          throw new Error(`See usage examples at: https://github.com/gomes042/gh-actions-dropbox`);
